{
  "name": "<%= name %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  "main": "./src/index.js",
  "files": [<% if (runkit) { %>
    "runkit.js",<% } %>
    "src"
  ],
  "scripts": {
    "eslint": "eslint src",
    "eslint-fix": "npm run eslint -- --fix",
    "test": "run-s testonly eslint",<% if (codecov) { %>
    "test-travis": "eslint src && jest --coverage && codecov",<% } %>
    "testonly": "jest",
    "build": "cheminfo build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cheminfo-js/<%= name %>.git"
  },
  "keywords": [],
  "author": "<%= userName %> <<%= email %>>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cheminfo-js/<%= name %>/issues"
  },<% if (runkit) { %>
  "runkitExampleFilename": "./runkit.js",<% } %>
  "homepage": "https://github.com/cheminfo-js/<%= name %>#readme",
  "jest": {
    "testEnvironment": "node"
  }
}
