'use strict';

const <%= camelName %> = require('..');

describe('<%= name %> test', function () {

    it('Something to test', function () {
        expect(<%= camelName %>()).toBe(true);
    });
});
