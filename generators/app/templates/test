'use strict';

const myModule = require('..');

describe('myModule test', () => {
  it('Something to test', () => {
    expect(myModule(42)).toBe(42);
    expect(myModule(42, { multiplier: 2 })).toBe(84);
  });
});
